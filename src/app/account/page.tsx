import Posts from "@/widgets/accountWidgets/posts/posts";

const Account = () => {
    return (
        <main className="container">
            <Posts/>
        </main>
    );
};

export default Account;